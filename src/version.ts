// Version string stamped into User-Agent headers and SDK metadata. Kept in
// one place so publish-time bumps flow through consistently. Update in sync
// with package.json — a future CI check could assert they match.
export const SDK_VERSION = "0.1.0";
export const SDK_NAME = "@withboundary/sdk";
