// Version string stamped into User-Agent headers and SDK metadata. Keep in
// sync with package.json; test/version.test.ts fails if release bumps drift.
export const SDK_VERSION = "0.4.0";
export const SDK_NAME = "@withboundary/sdk";
